/*
 * Copyright (c) 2012 Bartlomiej Siwek All rights reserved.
 */

#ifndef ELG_UTILITIES_FILESYSTEM_ASYNCFILE_H_
#define ELG_UTILITIES_FILESYSTEM_ASYNCFILE_H_

////////////////////////////////////////////////////////////////////////////////
// Someday... Maybe... If I have time...
////////////////////////////////////////////////////////////////////////////////

/*
#include "Elg/Utilities/Types.h"
#include "Elg/Utilities/Filesystem/FileAccessMode.h"

namespace Elg {
namespace Utilities {
namespace Filesystem {

class AsyncFile {
  // Consts
  static const elg_uint32 NPOS = 0xFFFFFFFF;

  // Constructor
  AsyncFile(const char* path, FileAccessMode access_mode, bool async_io);

  // Destructor
  ~AsyncFile(void);

  // Asynchronously reads from the file into a buffer
  // OsAsyncFileOperation ReadAsync(void* buffer, size_t length, size_t position);

  // Asynchronously writes from a buffer into the file
  // OsAsyncFileOperation WriteAsync(const void* buffer, size_t length, size_t position);
};

}  // Elg
}  // Utilities
}  // Filesystem
*/

#endif  // ELG_UTILITIES_FILESYSTEM_ASYNCFILE_H_
